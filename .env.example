ADMIN_USERNAME=admin
# 设置为自己的密码，至少 12 位。无默认密码。
ADMIN_PASSWORD=
# 使用 openssl rand -hex 32 生成。
SESSION_SECRET=
# 必须与浏览器地址一致（含 http/https 和非默认端口），结尾不加路径。
PUBLIC_ORIGIN=http://localhost:3000
PORT=3000
# 同机反向代理使用 127.0.0.1；局域网直接访问时改为 0.0.0.0。
BIND_ADDRESS=127.0.0.1
